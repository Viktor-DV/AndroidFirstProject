<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.AndroindFirstProject" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/design_default_color_primary_dark</item>
        <item name="colorPrimaryContainer">@color/design_default_color_primary_dark</item>
    </style>

    <!-- Light and dark theme-specific configurations can inherit from Base -->
    <style name="Theme.AndroindFirstProject" parent="Base.Theme.AndroindFirstProject" />
</resources>
